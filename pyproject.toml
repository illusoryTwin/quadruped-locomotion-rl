[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quadruped-locomotion-rl"
version = "0.0.1"
dependencies = []

[project.scripts]
train = "src.scripts.train:run"

[tool.setuptools.packages.find]
include = ["src*"]
